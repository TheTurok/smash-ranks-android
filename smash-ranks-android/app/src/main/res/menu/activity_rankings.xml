<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:compat="http://schemas.android.com/apk/res-auto">
    <item
        android:icon="@drawable/ic_search"
        android:id="@+id/activity_rankings_menu_search"
        android:title="@string/search"
        android:visible="false"
        compat:actionViewClass="android.support.v7.widget.SearchView"
        compat:showAsAction="collapseActionView|ifRoom" />
    <item
        android:icon="@drawable/ic_date"
        android:id="@+id/activity_rankings_menu_date"
        android:title="@string/last_rankings_update"
        android:visible="false"
        compat:showAsAction="ifRoom" />
</menu>
